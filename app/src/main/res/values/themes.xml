<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MyCasts" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <!-- Shown while the app is cold-starting, before installSplashScreen() hands control back to
         Theme.MyCasts in MainActivity.onCreate(). Uses the same brand orange and RSS+m glyph as
         the launcher icon; see ic_launcher_foreground.xml and colors.xml#splash_background. -->
    <style name="Theme.MyCasts.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.MyCasts</item>
    </style>
</resources>
